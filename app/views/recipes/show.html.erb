<h1>Todays recipe</h1>

<h1><%= @recipe.title %></h1>

<div>
  <%= raw(render_markdown(@recipe.content)) rescue simple_format(@recipe.content) %>
</div>

<%= link_to "Done",
  recipe_path(@recipe),
  data: { turbo_method: :delete, turbo_confirm: "Bon appétit" } %>
